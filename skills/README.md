# Local Skills

This directory contains repository-local skills packaged from reusable Phase 1 seams.

Current local skills:

- `offline-document-normalizer`
- `grounded-retrieval-toolkit`

These skills wrap stable script entrypoints and keep project-specific policy out of the reusable core as much as possible.
